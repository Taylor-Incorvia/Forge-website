# Normalise line endings so the repo is identical on Windows and CI (Ubuntu).
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.woff2 binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.mp4 binary
*.ico binary
